program stage2no210;  {improper uses of Pascallite features.}
  var x,y,z:integer;  why:boolean;
  begin
    if x then z:=0;    {predicate must be boolean valued}
	else  w := -3; else   {w not declared, extraneous else}
	while why do begin;
	  x := y + 2;
	  if x = 3 then
	  end;                       {improper nesting of if statement}
	else x := y;
  end.
