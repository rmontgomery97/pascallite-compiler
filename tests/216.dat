program stage2no216;  {improper uses of Pascallite features.}
  var x,y,z:integer;  why:boolean;
  begin
    x := 24;
	if x > 0 then  
	   z:=0;
	    
	else ;          {can use mull statement, though}
 
    while (x > y)   {missing do}
	 begin
	   z := x;
	   y := z;
	 end;
   end.
