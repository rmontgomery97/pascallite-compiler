program stage2no211;  {improper uses of Pascallite features.}
  var x,y,z:integer;  why:boolean;
  begin
    if why then
	   z:=0;    
	else 
	   w := -3; else   {w not declared, extraneous else}
	while why do begin;
	  x := y + 2;
	  if x = 3 then
	  end;                       {improper nesting of if statement}
	else x := y;
  end.
